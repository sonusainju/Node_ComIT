const height = 10;
const base = 4;
const perimeter = 2 * (height + base);
const area = height * base;

console.log(`The rectangle perimeter is ${perimeter}`)
console.log(`The rectangle area is ${area}`)
console.log(`The height length is ${height} and the base length is ${base}`);
