const Logger = require("logplease");
const c = Logger.create("utils");

c.debug(`Hello Node.js`);
c.info(`Node.js is great!!`);
c.warn(`Warning, Warning, I think we have a Warning`);
c.error(`Mayday Mayday, we have an errro, repeat.. we have an error`);
